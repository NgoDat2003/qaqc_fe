# Server-side only. Next.js rewrites /api/* and /uploads/* to this backend.
BE_INTERNAL_URL=http://localhost:3000

# Optional: run Playwright against a deployed or custom frontend URL.
# E2E_BASE_URL=http://localhost:3001

# Optional: override MSW/API base used by unit tests.
# TEST_API_BASE_URL=http://localhost:3000/api
